name: Publish Code
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'
          
      - name: Restore
        run: dotnet restore testzk/testzk.csproj
        
      - name: Build
        run: dotnet build testzk/testzk.csproj --configuration Release --no-restore
        
  publish:
    runs-on: ubuntu-latest
    needs: build 
    steps:            
      - name: Publish
        run: dotnet publish testzk/testzk.csproj  --configuration Release --no-build --output ./publish 
        
      - name: List directory contents
        run: ls -R ./publish  # Optional: List files in the publish directory for debugging
               
      - name: Upload publish artifacts
        uses: actions/upload-artifact@v3
        with:
          name: publish
          path: ./publish  
       
      - name: List Published Files
        run: dir ./publish
        
  artifacts:
    runs-on: ubuntu-latest
    needs: publish 
    steps:    
      - name: Upload publish artifacts
        uses: actions/upload-artifact@v3
        with:
          name: publish
          path: ./publish  
       
      - name: List Published Files
        run: dir ./publish
        
  deploy:
    runs-on: windows-latest
    needs: publish      
    steps:
      - name: Download Publish Artifacts
        uses: actions/download-artifact@v3
        with:
           name: publish
           path: testzk/testzk/myartifact.zip

      - name: Publish to on-prem VM
        run: |
          net use D: \\10.100.66.18\artifacts /user:${{ secrets.IIS_SERVER_USER }} ${{ secrets.IIS_SERVER_PASSWORD }}
          
          # Check if the artifact exists
          if exist D:\a\aspnetTestProject\aspnetTestProject\testzk\testzk\myartifact.zip 
          ( echo "Artifact exists"
          ) else (
            echo "Artifact does not exist"
            exit 1
          )
      
          # Extract the Artifact
          powershell -command "Expand-Archive -Path 'D:\a\aspnetTestProject\aspnetTestProject\testzk\testzk\myartifact.zip' -DestinationPath 'D:\artifacts\'"
      
          # Copy the artifact to the on-prem VM
          xcopy /Y /I D:\a\aspnetTestProject\aspnetTestProject\testzk\testzk\myartifact.zip* D:\artifacts
      
          # Clean up by deleting the mapped drive
          net use D: /delete
